# ===========================================
# HERMES - Configuration
# ===========================================
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# --- Chemins de base ---
BASE_DIR=${HOME}/hermes
ARCHIVES_DIR=${HOME}/hermes/archives

# --- Grafana Configuration ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme_admin_password
GRAFANA_UID=472
GRAFANA_GID=472

# --- InfluxDB Configuration ---
# InfluxDB 2.x utilise un token d'authentification
INFLUXDB_INIT_MODE=setup
INFLUXDB_INIT_USERNAME=admin
INFLUXDB_INIT_PASSWORD=changeme_influx_password
INFLUXDB_INIT_ORG=hermes
INFLUXDB_INIT_BUCKET=logs
INFLUXDB_INIT_RETENTION=30d
# Nom de la configuration CLI spécifique à HERMES (évite les conflits avec d'autres projets)
INFLUXDB_CLI_CONFIG_NAME=hermes-config
# Le token sera généré automatiquement au premier démarrage
# Vous pouvez le définir manuellement ici après la première installation
INFLUXDB_INIT_ADMIN_TOKEN=

# --- Loki Configuration ---
LOKI_UID=10001
LOKI_GID=10001

# --- Prometheus Configuration ---
PROMETHEUS_UID=65534
PROMETHEUS_GID=65534
PROMETHEUS_RETENTION=15d

# --- Promtail Configuration ---
PROMTAIL_UID=10001
PROMTAIL_GID=10001

# --- Réseau Docker ---
NETWORK_NAME=hermes_network

# --- Timezone ---
TZ=Europe/Paris
